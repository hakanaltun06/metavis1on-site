# Claude Code yerel ayarları (kişisel — repo'ya girmesin)
.claude/

# OS / Editor artifacts
.DS_Store
Thumbs.db
*.swp
*~

# Node (gerekirse — ilerideki tool entegrasyonları için)
node_modules/
npm-debug.log
yarn-error.log

# Yedek dosyaları
*.bak
*.tmp

# Local Firebase config — never commit real project credentials.
# (The example template lives at shared/config/firebase.local.example.js
#  and IS committed; only the real local copy is ignored.)
shared/config/firebase.local.js
